# 計分方式說明（SCORING.md）

本文件完整說明「人格座標 64」的計分演算法。實作位於 `src/lib/scoring.ts`，測試位於 `src/lib/__tests__/scoring.test.ts`。

## 題目結構

每題（`src/lib/types.ts` 的 `Question`）包含：

```ts
interface Question {
  id: string;            // 如 "EI01"，全題庫唯一
  text: string;          // 繁體中文題幹（日常情境描述）
  dimension: Dimension;  // 'EI' | 'SN' | 'TF' | 'JP' | 'AO' | 'HC'
  direction: 1 | -1;     // 1 = 同意偏向第一字母；-1 = 反向計分
  weight: number;        // 權重（0.5–2，目前全部為 1）
  pairId?: string;       // 語意配對題群組（穩定度計算用）
  category: 'work' | 'learning' | 'relationship' | 'daily' | 'decision' | 'stress';
}
```

每個維度 12 題、共 72 題。每題只測量一個維度。

## 作答轉換

五點 Likert 量尺轉為對稱數值（`convertAnswer`）：

| 作答 | 意義 | 轉換值 |
| --- | --- | --- |
| 1 | 非常不同意 | -2 |
| 2 | 不同意 | -1 |
| 3 | 不確定／視情況而定 | 0 |
| 4 | 同意 | +1 |
| 5 | 非常同意 | +2 |

超出 1–5 或非數字的值一律視為 0（中立），保證不崩潰。

## 反向題

`direction: -1` 的題目在計分時將轉換值乘上 -1。例如「熱鬧的活動結束後，我需要獨處恢復精力」是 E/I 維度的反向題：答「非常同意」（+2）會變成 -2，把分數推向 I。

每個維度至少 3 題反向計分，用來抵銷「一路同意」的作答習慣（acquiescence bias）。

## 權重與加總

每題分數 = `轉換值 × direction × weight`。

各維度分別加總為原始分 `raw[d]`，同時累計理論最大值 `max[d] = Σ(2 × weight)`（每維度 12 題、權重 1 時為 24）。

## 標準化

```
score[d] = round(raw[d] / max[d] × 100)   // 剪裁至 [-100, +100]
```

方向定義（正值一律偏第一字母）：

| 維度 | 正值 | 負值 |
| --- | --- | --- |
| EI | E | I |
| SN | S | N |
| TF | T | F |
| JP | J | P |
| AO | A | O |
| HC | H | C |

## 六維代碼產生方式

固定且可重現的規則（`letterOf` / `codeFromScores`）：

- `score >= 0` → 第一字母；`score < 0` → 第二字母
- 恰好 0 分時固定取第一字母（不使用隨機數）
- 前四維組成四字母，後兩維組成子型後綴：`INTJ-OC`

**相同答案永遠得到相同結果**——引擎是純函式，測試中對同一份答案重複計算 10 次驗證完全一致。

## 百分比計算

結果頁顯示每一側的百分比：

```
firstPercent  = round((score + 100) / 2)   // -100..100 → 0..100
secondPercent = 100 - firstPercent
```

例：`EI = +26` → E 63% / I 37%。

百分比代表**這次作答的傾向強度**，不是人格純度，也不得稱為「準確率」。

## 偏好強度

以 `|score|` 分三級（`strengthOf`）：

| |score| | 強度 |
| --- | --- |
| 0–14 | 偏好接近（頁面會標示「兩側偏好接近」） |
| 15–39 | 中度偏好 |
| 40 以上 | 明顯偏好 |

## 作答穩定度

每個維度有 2 組語意配對題（`pairId`），全站共 12 組。每組兩題計算「有效值」＝`轉換值 × direction`，兩題有效值之差的絕對值介於 0（完全一致）～4（完全矛盾）。

全部配對的平均差 `avg`：

| avg | 穩定度 |
| --- | --- |
| ≤ 1.0 | 高 |
| ≤ 2.0 | 中 |
| > 2.0 | 低 |

穩定度**不是準確率**，只表示語意相近／反向題之間的回答是否大致一致。穩定度低時，結果頁顯示：「你的部分回答呈現較大的情境差異。這不代表結果無效，但建議將報告視為探索起點，或在不同狀態下重新測驗。」

## 邊界情況

| 情況 | 行為 |
| --- | --- |
| 全部答 3（中立） | 各維度 0 分，依固定規則得 `ESTJ-AH`，不崩潰 |
| 全部答 1 / 全部答 5 | 分數互為相反數，皆在 [-100, 100] |
| 恰好 0 分 | 固定取第一字母（可重現） |
| `-0` | 正規化為 `0` |
| 缺答（未作答的題目） | 不計分（UI 層已強制答滿 72 題才能提交） |
| 非法作答值（0、6、NaN） | 視為 0，不崩潰 |
| 無任何配對題作答 | 穩定度回傳「中」 |
| 分享網址參數非法／代碼與分數矛盾 | `decodeResultParams` 回傳 null，頁面顯示友善錯誤（見 `src/lib/result-url.ts`） |

## 分享網址格式

```
/result?c=INTJ-OC&ei=-42&sn=-18&tf=25&jp=40&ao=-12&hc=-30&st=high
```

驗證規則：代碼須符合 `^[EI][SN][TF][JP]-[AO][HC]$`；六個分數必須是 -100～100 的整數；`st` 必須是 high/medium/low；且 `codeFromScores(scores)` 必須等於 `c`（防止竄改出矛盾頁面）。
