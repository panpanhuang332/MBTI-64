# 內容撰寫規範（CONTENT_GUIDE.md）

適用於題庫（`src/lib/questions.ts`）與 64 型文案（`src/lib/profiles/`）。

## 人格文案語氣

- 對「你」說話，溫和、具體、成年人對成年人的語氣
- 描述**偏好與傾向**（「你傾向…」「你常…」），不是判決（「你就是…」）
- 每一型都要寫出真實的優勢**與**真實的盲點；盲點用「可能」「容易」開頭，指向情境而非人格缺陷
- 成長方向要可操作（「練習把理想拆成本週可以開始的小步」），不是說教
- 允許溫度與幽默，但不油腔滑調；不用驚嘆號堆疊

## 禁用詞與禁用主張

**絕對不可出現：**

- 「準確率」「95%」「科學認證」「心理學認證」「最準」「官方 MBTI」「全球第一」
- 「最聰明」「最危險」「最成功」「最適合當領導」「最適合某職業」「最稀有所以最高等」
- 人口比例（「只有 2% 的人是…」）——沒有真實統計資料就不得顯示
- 診斷式語言：「你有～傾向症」「這代表你心理不健康」
- 決定論：「你註定」「你永遠無法」
- 職業／伴侶處方：「你適合當工程師」「你應該找 F 型伴侶」

**謹慎使用：**

- 「相對少見的組合」可以，但不得連結到優越性
- 比較兩型時只描述差異，不排高下

## 如何避免人格階級化

- 64 型在圖鑑中一律平等呈現：同樣的卡片大小、同樣的欄位、固定的代碼排序，沒有金字塔、天梯或稀有度標籤
- 每一型的欄位結構完全相同（優勢 4、盲點 4、工作、關係、壓力、成長、誤解、反思 3）
- 子型（A/O、H/C）描述為「不同的節奏與溫度」，明確寫出「O 不是優柔寡斷」「C 不是冷血」
- 命名採中性的自然／探索意象，避免帶價值序列的詞（王、神、天才、菁英）

## 如何避免負面標籤

- 盲點永遠與情境綁定：「變動太快時容易感到消耗」而不是「你抗拒改變」
- 每個盲點附近都給出出路（成長方向、練習）
- 「常見誤解」欄位主動替該型澄清刻板印象
- 壓力反應描述為「緊繃時可能…」的暫時狀態，不是本質

## 題目撰寫規則

- 日常情境化，不直接問「你內向嗎」
- 一題只測一個維度；避免雙重否定與過度抽象
- 不涉及性別、年齡、種族、宗教、政治立場，不設道德陷阱題
- 每維度 12 題：≥3 反向題、2 組語意配對題（`pairId`）、≥3 情境題
- 新增後執行 `npm run validate-data` 與 `npm run test`

## 如何新增類型描述

1. 修改核心型：編輯 `src/lib/profiles/cores.ts`（16 型，欄位缺一不可）
2. 修改子型：編輯 `src/lib/profiles/subtypes.ts`（4 型）
3. 64 型由程式組合生成，不需手寫；名稱格式為「子型名・核心名」（如「深林・星圖測繪者」），由 `getFullProfile` 自動組出
4. 執行 `npm run validate-data`：欄位缺漏、摘要長度超標、名稱重複都會報錯
5. 對照本文件的禁用詞清單自我檢查一次
